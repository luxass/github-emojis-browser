---
import PageLayout from "../../layouts/PageLayout.astro"
import {
  categories
} from "github-emojis/category";


export function getStaticPaths() {
  return categories.map((category) => ({
    params: {
      handle: category
    },
    props: {
      category
    }
  }))
}

const { handle } = Astro.params;
---

<PageLayout
  title=""
  description=""

>

</PageLayout>
